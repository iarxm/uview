# Agent instructions

Read [.AGENTS/README.md](.AGENTS/README.md) before changing this repository.
Keep changes small, retain the Bash style in `uview`, and do not alter the
preview protocol or desktop configuration without an explicit, tested plan.

Before handing off a code change, run `bash -n uview` and describe any manual
X11/tabbed verification that remains.
