File previews via tabbed xembed with persistent application states. For text mimes, neovim with it's server interface. For media files, mpv via it's ipc socket. For portable docs, Zathura via dbus.

Use `uview --zen-mode [FIFO [FILE]]` to start Bookokrat document previews in
zen mode. The default leaves Bookokrat's sidebar visible.
